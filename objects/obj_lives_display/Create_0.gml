//basically tracking the difference of where the UI is in comparison to the x and y of the camera
xDifference = x - camera_get_view_x(view_camera[0])
yDifference = y - camera_get_view_y(view_camera[0])